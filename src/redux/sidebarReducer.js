import React from 'react'

let initialState = {
    sidebarData: [
        { id: 1, name: 'Vlad' },
        { id: 2, name: 'Vanya' },
        { id: 3, name: 'Den' },
    ]
}
const sidebarReducer = (state = initialState, action) => {



    return state;
}
export default sidebarReducer
